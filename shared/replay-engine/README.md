# Shared Replay Engine

Reference projection logic from immutable events.

Entry point:
- `shared/replay-engine/src/index.ts`

Notes:
- MVP replay logic is intentionally simple.
- All devices/backend should converge by reusing equivalent replay rules.
